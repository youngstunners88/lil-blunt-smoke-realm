# Caffeine dispatch — 2026-08-29

Paste the block below into the Caffeine chat. Nothing else this round.

## Why this one

`marketing/aeo/crawl_gate.py` against the live site returns FAIL: none of the
five load-bearing claims are readable in the served HTML, and
`/troubleshooting/` answers 200 with the app shell rather than a page. The repo
carries the corrected copy but the repo is not the deployment path, so the
fixes are not live.

Two of these are accuracy problems, not just SEO ones:

- The site tells players to use the **arrow keys**, which are not bound in the
  build. A first-time player presses them, nothing happens, and they leave.
- The leaderboard sentence can be extracted away from its "no live on-chain
  leaderboard" disclaimer, so an answer engine can quote the claim without the
  caveat.

## The dispatch

```
Change only the static text content of the site. Do not convert to SSR or SSG,
do not add npm packages, do not add React Router routes, and do not change the
game canvas or any styling.

1. CONTROLS — the site currently says the arrow keys move the character. That
is wrong: the arrow keys are not bound in the build. Everywhere controls are
described (the how-to-play page, the docs page, llms.txt, and any FAQ or
structured-data answer about controls), replace them with exactly:

   Controls are A and D to move, W or Space to jump, J to attack, and K to dash.
   The arrow keys are not bound.

Also add Attack (J) and Dash (K) to any controls table, which currently omits
them.

2. HOMEPAGE — inside the existing <noscript> block, make sure these sentences
each appear as their own separate <p>, worded exactly like this:

   Lil Blunt: The Smoke Realm is a free 2D side-scrolling platformer you play in your web browser at smokegame.win.
   There is no download and no wallet.
   Controls are A and D to move, W or Space to jump, J to attack, and K to dash.
   Lil Blunt: The Smoke Realm is a browser game, not the music artist of a similar name.

Each must be its own paragraph. Do not merge them into a longer paragraph and
do not reword them.

3. LEADERBOARD WORDING — in the homepage <noscript> block, the sentence saying
best runs post to a wanted-board leaderboard must not appear without its
qualifier. Replace that sentence with:

   Scores are shown on a demonstration board styled as an old-west wanted
   poster; they are not recorded on a blockchain.

4. TROUBLESHOOTING PAGE — add a new static page at /troubleshooting/ served as
real HTML, not a client-side route. It must return different HTML from the
homepage when fetched directly. Content is in the repo at
src/frontend/public/troubleshooting/index.html — use that file's text and FAQ
structured data as-is. Add it to sitemap.xml and link it from the about,
docs and how-to-play pages.

5. LLMS.TXT LINK — add an ordinary, readable link to /llms.txt in the homepage
footer link list, with the text "llms.txt" and a title attribute reading
"A plain-text summary of this site written for AI assistants." Normal font
size. Do not hide it or shrink it.

Change nothing else.
```

## After it deploys

```bash
python3 marketing/aeo/crawl_gate.py     # must exit 0
```

If `/troubleshooting/` still shows "SPA fallback", step 4 was implemented as a
client-side route and needs redoing as a static file. Everything else can pass
while that one fails; it is worth a second dispatch on its own.
